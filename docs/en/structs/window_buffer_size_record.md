# WCI - Windows console interface

## [WCI](../wci.md) > Structure `WindowBufferSizeRecord`

Structure `WindowBufferSizeRecord` contains following fields:

- [`Coord`](./coord.md) `size`

---

Structure `WindowBufferSizeRecord` is used for recording events of
changing the size of window buffer for structure [`InputRecord`](./input_record.md).
