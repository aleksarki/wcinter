# WCI - Windows console interface

## [WCI](../wci.md) > Structure `FocusEventRecord`

Structure `FocusEventRecord` contains following fields:

- `bool` `setFocus`

---

Structure `FocusEventRecord` is used for recording focus events for structure [`InputRecord`](./input_record.md).
Such events are for internal use and should be ignored.
