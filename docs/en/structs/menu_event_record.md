# WCI - Windows console interface

## [WCI](../wci.md) > Structure `MenuEventRecord`

Structure `MenuEventRecord` contains following fields:

- `unsigned int` `commandId`

---

Structure `MenuEventRecord` is used for recording menu events for structure [`InputRecord`](./input_record.md).
Such events are for internal use and should be ignored.
